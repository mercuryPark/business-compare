export function IllustrativeBadge() {
  return (
    <span className="inline-flex items-center rounded-full border border-clay/30 bg-clay/10 px-2 py-0.5 text-xs font-semibold text-clay">
      교육용 예시 — 실제 통계 아님
    </span>
  );
}
